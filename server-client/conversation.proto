syntax = "proto3";

package common;

option java_package = "com.blemobi.demo.probuf";
option java_outer_classname = "ConversationProtos";

message PConversation {
	string conversationId = 1;     // 会话ID
	int64 openId = 2;              // 会话创建者
	int32 type = 3;                // 会话类型
	string icon = 4;               // 会话图标url地址
	string title = 5;	           // 会话名称
	int64 tag = 6;	               // 会话tag
	int32 memberLimit = 7;         // 群成员数
    int32 superGroup = 8;	       // 0非大群，1大群  
    string parentId = 9;           // 父会话id，默认0
    int32 nodeType = 10;           // 是否 父会话，0-否，默认；1-是父会话
    repeated int64 members = 11;   // 会话的成员列表openId
}

/*
 *@note 创建会话（单聊）
 *@path /chat/conversation/create/single
 *@method GET
 *@param token&uuid&chatId 登录用户信息（必须）
 *@param friend_uuid   好友uuid（非必须）
 *@param friend_chatId 好友chatId（必须）
 *@return 成功返回PConversation，失败返回PResult
 */
 
/*
 *@note 创建会话（群聊）
 *@path /chat/conversation/create/group
 *@method GET
 *@param token&uuid&chatId 登录用户信息（必须）
 *@param title 群聊名称（非必须）
 *@param icon  群聊头像url（非必须）
 *@param memberChatId 群组成员chatId数组（格式：memberChatId=1&memberChatId=2&memberChatId=3）（必须）
 *@return 成功返回PConversation，失败返回PResult
 */
 
/*
 *@note 增加会话成员
 *@path /chat/conversation/add/member
 *@method GET
 *@cookie token&uuid 增加会话成员的用户uuid，必须是群成员
 *@param conversationId 会话ID
 *@param uuidArray 需要增加的用户uuid数组   如：uuidArray=a&uuidArray=b&uuidArray=c
 *@return 返回PResult
 */
 
/*
 *@note 删除会话成员
 *@path /chat/conversation/remove/member
 *@method GET
 *@cookie token&uuid 删除会话成员的用户uuid，必须是群主
 *@param conversationId 会话ID
 *@param uuidArray 需要删除的用户uuid数组   如：uuidArray=a&uuidArray=b&uuidArray=c
 *@return 返回PResult
 */
 
/*
 *@note 修改会话名称
 *@path /chat/conversation/update/title
 *@method GET
 *@cookie token&uuid 
 *@param conversationId 会话ID
 *@param newTitle 会话新的名称
 *@return 返回PResult
 */
 
 /*
 *@note 修改会话头像
 *@path /chat/conversation/update/icon
 *@method GET
 *@cookie token&uuid 
 *@param conversationId 会话ID
 *@param newIcon 会话新的头像URL
 *@return 返回PResult
 */
 
 /*
 *@note 退出群聊（给全部群成员发送退出通知）
 *@path /chat/conversation/quit
 *@method GET
 *@cookie token&uuid
 *@param conversationId 会话ID
 *@return 返回PResult
 */
 
 /*
 *@note 静默退出群聊（只给群主发送退出通知）
 *@path /chat/conversation/quit/silent
 *@method GET
 *@cookie token&uuid
 *@param conversationId 会话ID
 *@return 返回PResult
 */
 
 /*
 *@note 修改会话群主
 *@path /chat/conversation/update/owner
 *@method GET
 *@cookie token&uuid
 *@param conversationId 会话ID
 *@param owner_uuid 要修改的群主uuid
 *@return 返回PResult
 */
 
 /*
 *@note 修改会话最大人数
 *@path /chat/conversation/update/memberLimit
 *@method GET
 *@cookie token&uuid 
 *@param conversationId 会话ID
 *@param memberLimit 会话最大人数，如200
 *@return 返回PResult
 */
 
 /*
 *@note 会话置顶
 *@path /chat/conversation/set/top
 *@method GET
 *@cookie token&uuid
 *@param conversationId 会话ID
 *@param isTop true:会话置顶，flase:取消会话置顶
 *@return 返回PResult
 */
 
 /*
 *@note 修改会话状态
 *@path /chat/conversation/update/status
 *@method GET
 *@cookie token&uuid 
 *@param conversationIdArray 需要修改的会话ID数组   conversationIdArray=a&conversationIdArray=b&conversationIdArray=c
 *@param status 0:隐藏，1:可见
 *@return 返回PResult
 */
 
 /*
 *@note 修改会话XPN状态
 *@path /chat/conversation/update/xpnStatus
 *@method GET
 *@cookie token&uuid 
 *@param conversationId 会话ID
 *@param status 0:接收通知，1:不接收通知
 *@return 返回PResult
 */
 
 /*
 *@note 解散会话
 *@path /chat/conversation/disbind
 *@method GET
 *@cookie token&uuid 必须是群主
 *@param conversationId 会话ID
 *@return 返回PResult
 */
 
 
 
 